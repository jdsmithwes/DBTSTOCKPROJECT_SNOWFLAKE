# Runtime dependencies for Python_Scripts/ and dbt
# Pinned to match local dev environment (Python 3.11)

# dbt (pins the connector version transitively to >=3.17.2,<4.0.0)
dbt-snowflake==1.10.3

# Snowflake connector — version controlled by dbt-snowflake; do not pin here
snowflake-connector-python>=3.17.2,<4.0.0

# AWS / S3 (backfill_missing_dates.py uploads to S3 before COPY INTO)
boto3==1.42.4

# HTTP (async AlphaVantage fetches, FRED, GDELT)
aiohttp>=3.9.0
requests>=2.31.0

# Data handling
pandas>=2.0.0

# ML — XGBoost model training (train_ml_model.py)
xgboost>=2.0.0
scikit-learn>=1.3.0
